package com.sergej.jornalapp

import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test

class ExampleUnitTest {
    @Test
    fun `given two plus two when calculating then returns four`() {
        assertEquals(4, 2 + 2)
    }
}
